{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main Quick Text Panel window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "clipboard-manager:default",
    "global-shortcut:default"
  ]
}

